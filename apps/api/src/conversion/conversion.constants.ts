/**
 * Shared constants for the conversion pipeline.
 * Kept in a separate file to avoid circular imports between
 * conversion.module.ts, conversion.service.ts, and conversion.processor.ts.
 */
export const CONVERSION_QUEUE = 'xlayout-conversion';
